[tool.pyright]
include = ["src"]
extraPaths = ["src"]
typeCheckingMode = "basic"

[tool.mypy]
python_version = "3.12"
mypy_path = "src"
explicit_package_bases = true

[tool.pyrefly]
search_path = ["src"]
project_includes = ["src", "tests", "validation"]
