# Tier B7-Docs: Documentation Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring 2 documentation claims back in sync with the current code: the `linear_regression`
Help Hub recipe undersells the coefficient table the report gained this session (GD9), and
README.md's "Repository Structure" tree lists 2 launcher script filenames that don't exist,
contradicting the correct names 86 lines earlier in the same file (GD10).

**Architecture:** Two independent, pure-text edits — no code behavior changes, so no TDD
red/green cycle applies here (there is no test to write for a documentation sentence). Each task
verifies its claim against the current, real state (the actual rendered report structure for
GD9, the actual files on disk for GD10) before editing, and re-reads the edit afterward to
confirm it's accurate.

**Tech Stack:** None (Markdown / Python docstring-embedded HTML).

---

### Task 1: GD9 — describe the linear_regression report's full coefficient table

**Files:**
- Modify: `src/core/help_content.py:488-490`

The `linear_regression` Help Hub recipe currently says the report only shows "the coefficient
(β) for the primary predictor" and R². `report_association.py`'s
`_build_linear_regression_coefficient_table_html` (confirmed wired end-to-end during the round-2
report/export audit, `docs/superpowers/audit-notes/release-2.0-audit/06-report-export-layer.md`)
now renders a full multi-row table — parameter, coefficient, SE, t, p-value, 95% CI — for every
predictor and covariate, not just the primary one.

- [ ] **Step 1: Confirm the coefficient table is genuinely wired end-to-end before editing the doc**

Run: `cd /Users/philippkrumm/Documents/BioMedStatX && git grep -n "coefficient_table" src/analysis/ src/export/`
Expected: a writer in `src/analysis/correlation_models.py` (or `clinical_models.py`) setting the
`coefficient_table` key, a reader in `src/export/report_association.py`
(`_build_linear_regression_coefficient_table_html`, confirmed during planning at line ~122), and
a dispatch call in `src/export/report_charts.py` gated on `model_type == "LinearRegression"`. If
any of these three links is missing, STOP — the feature this doc update describes may have
regressed since the audit; investigate before editing the doc to describe something not actually
true.

- [ ] **Step 2: Read the current recipe text**

Run: `sed -n '485,492p' src/core/help_content.py`

- [ ] **Step 3: Add a sentence describing the full coefficient table**

Using the Edit tool with the exact text read in Step 2 as `old_string` (read fresh — do not
assume the wording below is still byte-identical, since a Tier B GUI/docs batch running in
parallel could theoretically also touch this file), add one sentence after the existing R²
sentence. The addition:

```html
<p>The exported report also lists a full coefficient table — parameter, coefficient, standard error, t-statistic, p-value, and 95% confidence interval — for every predictor and covariate in the model, not just the primary one.</p>
```

placed immediately after the existing:

```html
<p>The report also shows <b>R²</b>: the share of variation in the outcome the model accounts for. A significant β with a low R² means the predictor matters but leaves most of the outcome unexplained.</p>
```

- [ ] **Step 4: Verify the file still parses as valid Python**

Run: `cd /Users/philippkrumm/Documents/BioMedStatX && python3 -c "import ast; ast.parse(open('src/core/help_content.py').read()); print('ok')"`
Expected: prints `ok`.

- [ ] **Step 5: Run the Help Hub test suite**

Run: `cd /Users/philippkrumm/Documents/BioMedStatX && python3 -m pytest tests/test_help_hub.py -v`
Expected: all pass, no change in count (this edit only adds an HTML string, doesn't change any
recipe's `id`/`keywords`/structure the test suite might check).

- [ ] **Step 6: Commit**

```bash
git add src/core/help_content.py
git commit -m "docs(help): describe the linear_regression report's full coefficient table"
```

---

### Task 2: GD10 — fix README's launcher-script self-contradiction

**Files:**
- Modify: `README.md:162-163`

`README.md:76` correctly links `start.sh` and `run.bat`. The "Repository Structure" tree 86
lines later (currently lines 162-163) instead lists `Start_BioMedStatX_on_Linux.sh` and
`start.bat` — neither of which exists on disk (confirmed via `ls *.sh *.bat` during planning:
only `start.sh` and `run.bat` are present).

- [ ] **Step 1: Confirm the actual files on disk before editing**

Run: `cd /Users/philippkrumm/Documents/BioMedStatX && ls *.sh *.bat`
Expected: exactly `run.bat` and `start.sh`. If any other launcher file now exists, STOP and
reconcile which name(s) are actually current before editing the doc.

- [ ] **Step 2: Read both the correct reference (line 76) and the wrong tree entries (lines
  162-163) fresh**

Run: `cd /Users/philippkrumm/Documents/BioMedStatX && sed -n '76p;160,165p' README.md`

- [ ] **Step 3: Fix the tree entries**

Using the Edit tool with the exact text read in Step 2 as `old_string`, change:

```
├─ Start_BioMedStatX_on_Linux.sh  # Launcher for Linux/macOS source/binary startup
├─ start.bat                      # Launcher for Windows source/binary startup
```

to:

```
├─ start.sh                       # Launcher for Linux/macOS source/binary startup
├─ run.bat                        # Launcher for Windows source/binary startup
```

- [ ] **Step 4: Verify both references now agree**

Run: `cd /Users/philippkrumm/Documents/BioMedStatX && grep -n "start.sh\|run.bat\|start.bat\|Start_BioMedStatX" README.md`
Expected: every remaining reference to a launcher script name is either `start.sh` or `run.bat`
— no `start.bat` or `Start_BioMedStatX_on_Linux.sh` left anywhere in the file.

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs(readme): fix Repository Structure tree's launcher script names"
```

---

## Self-review notes

- **Spec coverage:** GD9 (Task 1), GD10 (Task 2) — both findings assigned to this package are
  covered.
- **No contrived tests for pure-text edits.** Per this plan's own Architecture note, forcing a
  "failing test" for a documentation sentence would be artificial — each task instead verifies
  its factual claim against real, current state (Task 1 re-confirms the coefficient-table
  wiring; Task 2 re-confirms the actual files on disk) before editing, and re-checks afterward
  (Task 1 Step 5 re-runs the Help Hub test suite; Task 2 Step 4 re-greps for any remaining wrong
  name).
- **Both tasks re-read their target text immediately before editing** rather than trusting this
  plan's quoted snippets to still be byte-identical, since other Tier B packages
  (`docs/superpowers/specs/2026-07-07-audit-fix-clustering-design.md`'s B7-GUI package) touch
  files in the same general area and could in principle land first.
