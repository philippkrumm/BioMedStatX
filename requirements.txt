# Direct dependencies. Statistically result-relevant packages are pinned to
# exact versions so numeric output is reproducible across machines/CI.
# Full transitive freeze (including indirect deps) lives in requirements.lock
# — install that for a byte-for-byte reproduction of the audited environment.

# --- Statistically result-relevant: pinned ---
numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
statsmodels==0.14.6
pingouin==0.6.1          # central engine: rm/mixed/welch ANOVA, sphericity
scikit-posthocs==0.12.0  # Dunn / Games-Howell post-hoc
scikit-learn==1.8.0
marginaleffects==0.5.0   # EMM contrasts (does not expose __version__)

# --- Rendering / export: pinned ---
jinja2==3.1.6            # was 3.1.4 (CVE-2024-56326 / CVE-2025-27516); now matches installed
plotly==6.6.0            # was 5.24.1; reconciled to installed env (rendering-only)

# --- Plotting / IO / UI: floating (no effect on numeric results) ---
matplotlib
openpyxl
seaborn
PyQt5
xlrd
pillow
networkx
requests
packaging

# Optional / alternatives:
# PySide2  # use instead of PyQt5 if preferred
